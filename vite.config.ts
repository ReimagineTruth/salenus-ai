import { defineConfig } from "vite";
import react from "@vitejs/plugin-react-swc";
import path from "path";
import { componentTagger } from "lovable-tagger";

// https://vitejs.dev/config/
export default defineConfig(({ mode }) => ({
  base: '/',
  server: {
    host: "localhost",
    port: 3000,
    open: true,
    historyApiFallback: true,
    strictPort: false,
    // Add headers to allow iframe embedding on Pi Network subdomains
    headers: {
      'Access-Control-Allow-Origin': '*',
      'Access-Control-Allow-Methods': 'GET, POST, PUT, DELETE, OPTIONS',
      'Access-Control-Allow-Headers': 'Content-Type, Authorization, X-Requested-With',
      'Access-Control-Allow-Credentials': 'true',
      // Remove X-Frame-Options to allow iframe embedding
      'X-Frame-Options': 'SAMEORIGIN',
      // Alternative: Allow embedding from Pi Network domains
      'Content-Security-Policy': "frame-ancestors 'self' https://*.pinet.com https://*.minepi.com https://salenus.xyz"
    }
  },
  plugins: [
    react(),
    mode === 'development' &&
    componentTagger(),
  ].filter(Boolean),
  resolve: {
    alias: {
      "@": path.resolve(__dirname, "./src"),
    },
  },
  build: {
    rollupOptions: {
      output: {
        manualChunks: {
          vendor: ['react', 'react-dom'],
          router: ['react-router-dom'],
        },
      },
    },
  },
}));
